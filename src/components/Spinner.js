import React from 'react';
import spinerGIF from '../img/stable/spinner.gif';
const Spinner = () => {
  return <img className="spinner" src={spinerGIF} alt="Loading..." />;
};

export default Spinner;
